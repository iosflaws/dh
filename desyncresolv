wait(1.2)
game.StarterGui:SetCore("SendNotification", {
Title = "Aurora.vip"; -- the title (ofc)
Text = "Desync Resolver Enabled. (THIS IS IN EARLY PROGRESS)"; -- what the text says (ofc)
Duration = 10; -- how long the notification should in secounds
})

LocalPlayer.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character:FindFirstChild("Humanoid") and LocalPlayer.Character:FindFirstChild("Humanoid").Health > 100
